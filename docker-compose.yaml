version: "3.9"

services:
  app:
    image: .
    working_dir: /app
    volumes:
      - sqlite_data:/app/db_data
    ports:
      - "8080:8080"
    environment:
      HTTP_PORT: 8080

volumes:
  sqlite_data:
